# P0.17 Excel MCP path guidance — 2026-09-12

Runner: `p0-wave1-20`. Nothing was moved; no Excel taskpane or shared gateway was started or restarted.

## Change

`README.md` now uses the real lowercase `ai-excel-addin` name and `cd "$JUPYTER_ROOT/ai-excel-addin"`, with the checkout-forest binding explained. Existing non-path startup instructions are intentionally unchanged in this path-only unit.

This note is committed with the change. Its enclosing commit is indexed by full SHA in `ai-excel-addin/docs/qa/jupyter-p0-p0-17-2026-09-12.md`.

## Exact acceptance

With `JUPYTER_ROOT=/Users/henrychien/Documents/Jupyter`, from disposable scratch cwd:

```bash
cd "$JUPYTER_ROOT/ai-excel-addin" && pwd
```

Observed exit 0 and:

```text
/Users/henrychien/Documents/Jupyter/ai-excel-addin
```

`test -d /Users/henrychien/Documents/Jupyter/ai-excel-addin` also exited 0. This is the edited checkout citation; the unchanged `npm run dev-server` / `npm start` commands were not run against the shared Excel surface.

To exercise the README's package installation and safe CLI entrypoint, copied `python/` to `/var/folders/h8/nl9tqwb95cgbyvt811z97dl00000gn/T/jupyter-p0-p0-17-o2924b1t/excel-checkout/python` (excluding generated caches, egg-info and venvs). Created the disposable interpreter with:

```text
python3 -m venv /var/folders/h8/nl9tqwb95cgbyvt811z97dl00000gn/T/jupyter-p0-p0-17-o2924b1t/excel-venv
```

`python3 --version` was `Python 3.13.2`. With that venv's `bin` first on PATH, ran the exact advertised argv from the scratch checkout:

```bash
pip install -e ./python
```

Observed exit 0; key output:

```text
Successfully built excel-mcp
```

The successful install included `excel-mcp-0.1.0`. From scratch cwd, ran:

```bash
excel-mcp --help
```

Observed exit 0; key output:

```text
usage: excel-mcp [-h] {setup,start,mcp,generate-mcp-key} ...
```

Exact import-path probe:

```text
/var/folders/h8/nl9tqwb95cgbyvt811z97dl00000gn/T/jupyter-p0-p0-17-o2924b1t/excel-venv/bin/python -c 'import excel_mcp; print(excel_mcp.__file__)'
```

Observed:

```text
/private/var/folders/h8/nl9tqwb95cgbyvt811z97dl00000gn/T/jupyter-p0-p0-17-o2924b1t/excel-checkout/python/excel_mcp/__init__.py
```

No live venv was changed. No formatter, linter, or test suite was run. The disposable install, safe CLI invocation, and checkout resolution are the scoped proof.
